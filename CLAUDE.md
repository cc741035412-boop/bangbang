# 帮帮师记项目规则

> 项目唯一位置：当前 Git 仓库根目录；不得另建工作副本。
> 后端源码：`backend/`　前端源码：`frontend/`
> 禁止在其他目录创建项目副本。每次会话开始前先确认当前工作区是此路径。

## 验证与测试的铁律

- 严禁对 `backend/bangbang.db` 执行任何写操作（POST/PATCH/DELETE）。
- 所有写操作验证必须在独立临时数据库上进行，通过环境变量指定 DB 路径与服务端口（默认 8001）。
- 对 `bangbang.db` 只允许只读请求（GET）。
- 违反此规则产生的脏数据，清理需要用户逐条授权，成本很高。

## 1. 产品目标

帮帮师记帮助幼儿园主班教师，把日常照片和视频沉淀为可追踪、可复用的结构化观察素材。

核心闭环：

1. 教师上传手机照片或视频，选择游戏区域；班级与年龄段由系统带入
2. 主观察幼儿单选、同时记录的其他幼儿多选，明确显示文字角色；可预选观察幼儿；生成白描后按人物的衣着、行为线索对应姓名，同一人物只选一次
3. 有预设目标：多选或添加观察目标，确认后自动生成有重点的客观白描；没有预设目标：点击“先看看素材”，生成客观白描后再选择或添加目标
4. 教师核对白描；推理模型必须同时接收匿名观察对象、观察目标、白描、区域和年龄段，再推荐指标
5. 教师逐条采纳、否决或自行补充指标
6. 教师填写观察分析与支持策略，两项均为必填，完成后才能确认、导出
7. 教师确认后形成正式观察记录，预览并导出

观察目标只决定关注重点，不充当行为证据；模型不得倒推教师意图。目标、幼儿或白描变化后重新推荐，历史候选和教师决定保留用于审计，不作为新版结果复用。白描人物归组只提供待核对的线索，姓名由教师确认；群体泛称不映射为单人。人物确认接口原子保存姓名替换及观察对象，避免分步保存产生重复生成。幼儿选择立即在本机反馈，后台串行保存并合并连续选择；保存失败保留选择供重试，AI 操作前必须等最后一次选择保存完成。主观察幼儿切换后，旧主体的白描与推荐上下文必须失效。指标卡以采用/不采用为唯一选择入口，卡内即时反馈、保存中与失败状态必须清晰。自定义观察目标本轮随记录保存，不建立跨记录目标库。沿用现有表和 AI 审计 JSON，不做 schema 变更。

产品不是“让 AI 代写观察记录”。AI 负责客观白描与候选建议，教师对指标、分析和最终记录负责。

## 2. 目录约定

| 目录 | 用途 | 约束 |
|---|---|---|
| `backend/` | FastAPI 后端源码、后端测试与依赖声明 | 运行时数据库和上传素材不得提交 |
| `frontend/` | 手机端 Web/PWA 源码 | 建立前先补充该目录技术约定 |
| `docs/` | One-Pager、用户流程、接口契约、测试记录 | 文档名使用小写英文与连字符 |
| `deploy/` | 公网演示与托管平台的构建入口 | 只放可复现配置，不放运行数据、域名私钥或平台 token |
| `work/` | 临时分析、草稿和中间产物 | 可随时清理，不作为正式交付 |
| `outputs/` | 面向用户的最终交付物 | 不放源码和运行时数据 |

新增一级目录前，必须先在本文件补充用途和约束。源码不得散落在项目根目录。

## 3. 当前技术

- 后端：Python 3.9、FastAPI、SQLModel、SQLite、Uvicorn
- AI：工作流 A 支持模拟与豆包视觉；工作流 B/C 可通过 `BANGBANG_AI_MODE=mock|deepseek` 切换，DeepSeek 失败降级到带标识的规则草稿
- 前端：React 19、TypeScript 5、Vite 8、React Router 7（声明式模式）、TanStack Query 5、Tailwind CSS 4

## 4. AI 与数据规则

1. 客观白描由 AI 主笔、教师核对；禁止无证据推测幼儿意图、能力或情绪。
2. 指标对齐由 AI 提供候选，教师做最终判断。
3. 分析与措施由教师主笔，AI 最多提供思路支架。
4. 系统纯计算结果与 AI 建议必须分开返回和展示。
5. 没有明确行为信号时允许弃权，禁止硬猜。
6. 不自动判定“社会情感”维度。
7. 所有 mock 返回必须带 `is_mock: true` 和用户可见提示。
8. `ObservationTag.source`、`accepted`、`confidence` 是效果评估核心字段，任何重构都必须保留。
9. 真实幼儿姓名、照片、视频不得进入测试数据库、代码仓库和日志。
10. 测试统一使用虚构姓名和模拟素材。

## 5. 开发纪律

1. 一次只开发一个可验证的小功能。
2. 修改前先阅读本文件及目标目录中的规则。
3. 修改后主动运行与风险相称的验证，不通过不得宣称完成。
4. 不通过注释报错或增加绕过标记让代码表面通过。
5. 数据库结构变更必须先说明影响，并通过迁移脚本完成；不得直接重建数据库。
6. 未经确认，不删除文件或目录，不修改 `.env`、密钥、token、CI/CD，不执行部署、发布、`git push`、`git rebase` 或破坏性 Git 命令。
7. 依赖必须写入项目依赖文件；不得依赖未声明的全局包。
8. 上传文件使用程序生成的唯一文件名，不使用幼儿姓名命名。

## 6. 后端完成标准

- 服务能正常启动
- `/health` 返回成功
- `/docs` 能打开
- 新接口返回正确状态码
- 数据能正确写入或读取
- 原有接口仍然正常
- `python demo.py` 能跑通完整闭环

## 7. 当前阶段范围

当前个人作品集整理阶段优先完成两件事：

1. 将已验证的手机端闭环整理为脱敏源码审阅内容，保留既有功能与开发历史。
2. 交付用于个人网站的产品案例与静态模拟流程草稿，审阅并确认公开范围后再推送和发布。可安装 PWA 是已有形态，长期在线体验另行确认。

暂不优先开发园长后台、复杂组织权限、期末 PPT 和成长档案汇编。公开体验版不是正式生产系统，不承诺承载园所真实业务数据。

## 8. 公开体验版规则

1. 公开环境只允许使用虚构幼儿、模拟素材和演示账号，不得上传真实幼儿姓名、影像、教师隐私或园所内部材料。
2. 所有幼儿、教师、素材、观察记录、导出和 AI 操作接口必须校验登录态，并校验当前账号的园所、班级或记录归属；不得保留“公网匿名兼容”路径。
3. 体验环境与本地真实数据完全隔离，不复制 `backend/bangbang.db`、`backend/uploads/`、`.env`、日志、导出文件或备份文件。
4. 体验数据必须有明确的保存期限和清理机制；清理策略在部署文档中说明，执行删除仍遵守用户授权规则。
5. 用户协议、隐私说明和“仅供模拟体验”提示必须从注册页可访问，且明确说明 AI 输出不等于教师专业结论。
6. 对外 README 不提供真实验证码、密钥、生产域名内部配置或真实数据恢复方式。

## 9. PWA 与部署边界

1. 第一阶段 App 形态为可安装 PWA：具备 manifest、应用图标、独立窗口显示、安装说明和基础离线壳；不把网络写入伪装成离线可用。
2. iOS App Store、Android 商店和原生安装包不属于第一阶段；如后续采用 Capacitor 或其他原生封装，必须先补充对应平台、签名、权限、隐私和发布规则。
3. 前端生产环境不得依赖 Vite 开发代理；API 地址和跨域策略必须通过公开配置统一管理。
4. SQLite 与本地上传目录只能部署在具有持久磁盘且明确单实例约束的体验环境。若需要多实例或正式业务使用，必须先设计数据库迁移和对象存储方案，未经确认不得实施 schema 迁移。
5. Linux 部署不得依赖 macOS Quick Look；视频探测、抽帧和缩略图能力必须使用服务器可声明安装的跨平台依赖，失败时提供可理解的降级界面。
6. 部署前必须验证：全量测试、生产构建、空环境初始化、核心端到端流程、375px/390px 页面、权限隔离、上传限制和敏感文件检查。
7. 部署配置、CI/CD、域名、密钥和公开发布仍属于红线操作，必须在实际修改或执行前单独获得用户确认。

## 10. Git 发布整理规则

1. 发布提交只记录当前确认后的最终文件状态，不提交编辑过程中的临时版本。
2. 提交前同时检查暂存区与工作区；同一文件存在两层修改时，以验证通过的工作区最终版本重新暂存，禁止直接提交旧暂存快照。
3. 永不提交数据库及备份、上传素材、真实导出、`.env`、密钥、token、运行日志、缓存和 `work/` 中间产物。
4. 每个提交保持单一目的，提交说明写清产品能力或规则变化；公开发布前从发布分支完成验证，再按用户确认决定是否合并到 `main` 和推送。

## 11. 个人作品集与脱敏交付

1. 用户最新明确授权：将当前项目提交并推送到现有 GitHub 仓库，并将该仓库设为公开；后续已明确选择 Apache License 2.0，授权添加完整 LICENSE、同步许可说明并提交推送。许可文件位于仓库根目录，使用官方完整文本；第三方依赖与引用材料保留原有权利和来源说明。本次授权不包含个人网站部署、购买服务、删除本机文件或重写 Git 历史。执行前核查当前文件与可达历史，不重复询问同一推送和公开操作。
2. `docs/portfolio-case-study.md` 是作品页文案源稿；`outputs/portfolio-preview.html` 是独立、无外部依赖的静态预览，不接入真实账号、素材或 AI 服务，不改变 App 页面。
3. `work/release-review/` 存放本地审查脚本、脱敏命中摘要、历史审查及文件清单；不得记录密钥原文、个人联系方式或复制业务数据。
4. `outputs/bangbang-source-review.zip` 是按明确文件清单生成的源码审阅包，不是项目工作副本或正式发行版；不包含 `.git`、运行数据、内部交接资料、旧素材、旧打包文件或环境文件。只允许无凭据的 `.env.example`。
5. 文档只陈述已有功能与可核对的验证结果；研究目标、模拟交互、尚未实现的能力必须明确区分。模拟作品页不宣称真实 AI 生成。
6. 指标库来源授权与项目许可证未明确时，记录为公开源码发布前待确认事项，不擅自声明开源许可、重写历史或删除文件。
7. 用户已纠正项目背景：这是前公司工作项目，不是个人原创产品实践。公开作品页暂用“幼儿观察记录工具”等描述性名称，隐藏产品品牌与公司标识；个人职责、任职期间交付与后续原型迭代的时间边界需据用户确认填写，不能将当前开发或试用全部归入原公司成果。
8. 作品页采用少量说明配三个一一对应的案例：目标驱动、证据核对、主次切换。示例全部用虚构代号与程序重绘，不使用课题指标库原文或公司素材。静态示例标明为当前原型的交互重绘，不代表原公司上线页面或实测成效。
9. 用户已在说明前公司背景之后明确要求公开现有仓库，并随后选择 Apache License 2.0；保留来源说明，不将用户操作指令表述为公司授权证明或第三方材料的再授权。个人网站的职责、经历时间与案例公开范围仍单独确认。
